[Definition]
# e.g. t=2025-09-10T12:42:12.612369279Z
datepattern = ^t=%%Y-%%m-%%dT%%H:%%M:%%S(?:\.%%f)?Z

# 기본적인 로그인 실패
failregex = ^.*lvl=eror.*msg="Invalid username or password".* remote_addr=<HOST>.*$
            ^.*lvl=eror.*msg="Failed to authenticate user".* remote_addr=<HOST>.*$

# (보강) API 키/토큰/쿠키 인증 실패, BasicAuth 실패·만료
            ^.*lvl=eror.*msg="Invalid API key".* remote_addr=<HOST>.*$
            ^.*lvl=eror.*msg="Failed to look up user based on cookie".* remote_addr=<HOST>.*$
            ^.*lvl=eror.*msg="user token has expired".* remote_addr=<HOST>.*$
            ^.*lvl=eror.*msg="Basic authentication failed".* remote_addr=<HOST>.*$

ignoreregex =
